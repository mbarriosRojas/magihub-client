# La publicación de la imagen de magihub-client se movió a GitHub Actions
# (.github/workflows/publish.yml en github.com/mbarriosRojas/magihub-client),
# igual que hace el proyecto original de LibreChat — así no requiere ningún
# token/credencial manual (GITHUB_TOKEN lo provee GitHub automáticamente).
#
# Este repo de Bitbucket sigue siendo un mirror del código para mantener el
# mismo flujo que los demás repos magihub-*, pero el build+push de la imagen
# Docker se dispara solo en GitHub.
